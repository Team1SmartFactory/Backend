# FastAPI
APP_NAME="Smart Factory Inventory Backend"
DEBUG=true

# MQTT 브로커 (Mosquitto) 접속 정보 — 로봇/센서 어댑터와의 공통 계약
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883

# DB 연결 문자열 — 개발 단계는 SQLite, 통합 단계는 PostgreSQL로 전환
DATABASE_URL=sqlite:///./dev.db
