# 개발 단계 전용 설정 — 인증 없이 로컬에서 바로 pub/sub 가능하게 한다.
# 실기/배포 단계에서는 사용자 인증(password_file) 또는 TLS를 추가해야 한다.

listener 1883

listener 9001
protocol websockets

allow_anonymous true

persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log
